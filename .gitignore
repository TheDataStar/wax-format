# Rust build artifacts
/target
/crates/*/target
/fuzz/target

# cargo-fuzz working state (seed corpora ARE committed, see fuzz/corpus/)
/fuzz/artifacts
/fuzz/smoke-corpus
/fuzz/coverage

# editor / OS
*.swp
.DS_Store

# wax-builder emits a build report beside every archive it writes;
# the committed legacy fixtures do not need theirs tracked.
*.wax.build-report.json
