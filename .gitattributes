# Shell scripts must keep LF endings. They are run on the Linux rigs, and a
# CRLF checkout breaks the shebang there ("bad interpreter: no such file or
# directory"). This repo is worked on from Windows, where core.autocrlf=true
# would otherwise convert them on checkout.
*.sh text eol=lf
